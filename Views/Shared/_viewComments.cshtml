<!-- 
    [description]
        partial view that renders a list of comments for the selected article 
    [todo]
        [x] add link to delete comment
-->
<div>
    <h3>View Comments</h3>      
    <div>
    @foreach (var item in Model.comments) {
        <strong>@item.Title</strong><br>
        <p>@item.Content</p>
        // display commentManager bar if ] user is logged in, Manger includes options to ] delete, ] edit 
         @if (@User.Identity.IsAuthenticated){
            <a asp-controller="Comments" asp-action="Delete" asp-route-id=@item.Id> Delete</a>

            <a asp-controller="Comments" asp-action="Edit" asp-route-id=@item.Id> Edit</a><br>
         }
    }
    </div>
</div>

